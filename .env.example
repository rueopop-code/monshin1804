# ─── monshinsupply API Config ───

# Port (Railway จะ set ให้อัตโนมัติ ไม่ต้องแก้)
PORT=3000

# Secret key สำหรับ Admin (เปลี่ยนให้ซับซ้อนก่อน deploy!)
API_SECRET=monshin-secret-change-me

# Path ของ SQLite database
DB_PATH=./data/products.db
